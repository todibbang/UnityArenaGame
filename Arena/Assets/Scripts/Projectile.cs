using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Projectile : MonoBehaviour {
    /*
    public float Speed;

    GameObject TargetGameObject;
    Transform StartPosition;
    Vector3 TargetPosition;

    public void Prepare(GameObject target, Transform start)
    {
        TargetGameObject = target;
        StartPosition = start;
    }

    public void Prepare(Vector3 clickPosition, Transform start)
    {
        var x = clickPosition.x - start.position.x;
        var z = clickPosition.z - start.position.z;
        StartPosition = start;
        TargetPosition = new Vector3(start.position.x + (x * 10), 1.5f, start.position.z + (z * 10)); 
    }

    void Start()
    {
        transform.position = StartPosition.position;
    }

    // Update is called once per frame
    void Update () {

        
    } */
}
